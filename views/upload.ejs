<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/upload.css">
  </head>

  <body>
    
    <%- include("partials/header") %>
    <%- include("partials/message") %>

    <div class="pagina">
      

      <fieldset id="login_box">
        <h1>UPLOAD</h1>
        <form action="/upload" method="POST" enctype="multipart/form-data">
          <label>Disciplina:</label><br>

          <select name="disciplinaID" class="inputbox" required>
            <% disciplinas.forEach(d => { %>
              <option value="<%= d._id %>"><%= d.nomeDisciplina %> (<%= d._id %>)</option>
            <% }) %>
          </select><br><br>

          <label>Arquivo (PDF ou vídeo):</label><br>
          <input type="file" name="file" accept=".pdf,video/*" class="inputbox" required><br><br>

          <button type="submit" class="button">Enviar</button>
        </form>
      </fieldset>

    <%- message %>

  </body>
</html>